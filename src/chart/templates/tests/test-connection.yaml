apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "casha.fullname" . }}-test-connection"
  labels:
    {{- include "casha.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "casha.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
